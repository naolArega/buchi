version: '3'

services:
  web:
    build: .
    ports:
      - 8000:8000
    volumes:
      - cdn:/app/cdn
  mongodb:
    image: mongo:5.0
    ports:
      - 27017
    volumes:
      - data:/mongo/data

volumes:
  data:
  cdn:
